// map function example ( find the square of the array )
let nums =[1,2,3,4,5,6];
arr.map((val,indx)=>{
    console.log(val*val,indx); // this will print the square of the each value of an array and the index value
});